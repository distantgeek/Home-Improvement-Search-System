# Meilisearch
MEILI_MASTER_KEY=change-me-at-least-16-chars

# Frontend API key — actions: ["search", "stats.get"], indexes: ["events"]
# Create after first deploy:
#   curl -X POST http://localhost:7700/keys \
#     -H "Authorization: Bearer $MEILI_MASTER_KEY" \
#     -H "Content-Type: application/json" \
#     -d '{"name":"HISS Frontend Key","actions":["search","stats.get"],"indexes":["events"],"expiresAt":null}'
# Then set this to the returned "key" value:
MEILI_KEY=

# Meilisearch URL for the browser (LAN-accessible host:port)
MEILI_URL=http://192.168.2.148:7700

# Bind Meilisearch port to this IP only (set to LAN IP in prod, 0.0.0.0 for dev)
MEILI_HOST=192.168.2.148

# API keys
SERPER_API_KEY=
EVENTBRITE_API_KEY=

# Pipeline schedule (cron, default: weekly Sunday 3am)
PIPELINE_SCHEDULE=0 3 * * 0

# Set to 'true' to test pipeline without writing to Meilisearch or SQLite
DRY_RUN=false

# NPM external Docker network name (run: docker network ls)
NPM_NETWORK_NAME=your_npm_network_name
