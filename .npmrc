# Security: pin exact versions, always generate lockfile, always audit
save-exact=true
package-lock=true
audit=true

# After any install: npm audit signatures
# Checks all packages have verified registry signatures + SLSA attestations
